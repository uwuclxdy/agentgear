---
description: Greet the user and summarize what the kitchen-sink plugin ships.
---

Greet the user, then list the components this plugin installed: an MCP server, a
`SessionStart`/`UserPromptSubmit` hook pair, this command, a reviewer subagent, and a
demo skill.
