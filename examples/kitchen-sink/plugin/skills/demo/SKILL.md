---
name: demo
description: Demonstration skill bundled with the kitchen-sink example plugin.
---

# Demo skill

This skill exists so the example ships a real `skills/<name>/SKILL.md`. Claude Code
loads it like any other skill; the non-CC backends skip skills for now (no stable
file surface), so it is present but inert outside Claude Code.
