---
description: List the coding-agent backends multi-installer can target.
---

List the coding-agent backends this plugin's derive declares, then note that
`multi-installer setup --agent <id>` narrows install to any one of them
(`multi-installer status` shows which are detected on this machine).
