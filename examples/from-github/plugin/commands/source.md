---
description: Report where this plugin came from and how it updates.
---

Tell the user this plugin was installed from the `uwuclxdy/agentgear` GitHub
marketplace rather than a blob baked into the binary, so `claude plugin update`
picks up new plugin versions without waiting on a new binary release.
