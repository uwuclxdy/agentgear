---
name: ez-helper
description: fixture subagent so the plugin exercises an agent-def component
model: sonnet
---

A fixture helper agent. It exists only to give the fixture plugin a real
`agents/**/*.md` component for the components IR + each backend to translate.
